apiVersion: v1
kind: Service
metadata:
  name: wikijs-nodeport
  namespace: wikijs
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: wiki
    app.kubernetes.io/instance: wikijs
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
      nodePort: 32094
